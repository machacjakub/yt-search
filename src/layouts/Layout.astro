<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Basics</title>
	</head>
	<body>
		<div class="background-container">
			<div class="blurred-circle"></div>
		</div>
		<slot />
	</body>
</html>

<style>
	:root {
		--bg-color: #FFFFFF;
		--input-bg: rgba(255, 255, 255, 0.2);
		--input-border: #CCCCCC;
		--input-focus-border: #FF0000;
		--input-focus-shadow: rgba(255, 0, 0, 0.5);
		--text-color: #000000;
		--heading-color: #000000;
		--description-color: #666666;
		--placeholder-color: #888888;
		--button-bg: #FF0000;
		--tag-bg: #666666;
		--favorite-bg: #DAA300;
		--button-hover: #CC0000;
		--input-box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1), 0 0 10px rgba(255, 0, 0, 0.05);
		--dropdown-bg: rgba(255, 255, 255, 0.6)
	}

	@media (prefers-color-scheme: dark) {
		:root {
			--bg-color: #0F0F0F;
			--input-bg: #121212;
			--input-border: #303030;
			--input-focus-border: #FF0000;
			--input-focus-shadow: rgba(255, 0, 0, 0.5);
			--text-color: #FFFFFF;
			--heading-color: #FFFFFF;
			--description-color: #AAAAAA;
			--placeholder-color: #AAAAAA;
			--button-bg: #FF0000;
			--tag-bg: #666666;
			--favorite-bg: #DAA300;
			--button-hover: #CC0000;
			--input-box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1), 0 0 10px rgba(255, 0, 0, 0.05);
		}
	}
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		background-color: var(--bg-color);
	}

	.background-container {
		position: fixed;
		width: 100%;
		height: 100vh;
		background-color: #fafafa;
		overflow: hidden;
	}

	.blurred-circle {
		position: fixed;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		width: 400px;
		height: 400px;
		border-radius: 50%;
		background-color: rgb(166, 48, 48);
		opacity: 0.6;
		filter: blur(180px);
	}
</style>
